// Primary image generation provider.
// 'kie'    → Kie.ai is used directly (Google skipped)
// 'google' → Google Gemini is used first, Kie.ai as fallback
export const PRIMARY_PROVIDER: 'kie' | 'google' = 'kie';
